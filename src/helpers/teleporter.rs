pub fn compute_destination() {
  //so I need to catch the operation happening when use teleporter is called
  //reimplement the operation here
  //set the 8th register to the value resulting from the operation
}
